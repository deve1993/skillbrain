{
  "skill": "astro",
  "version": "1.0.0",
  "test_cases": [
    {
      "id": "content-collections-setup",
      "description": "Verifies the skill creates a properly typed Content Collection schema using Zod",
      "input": "Create a blog Content Collection in Astro with title, description, pubDate, author, and optional tags",
      "expected_output_contains": [
        "defineCollection",
        "z.object",
        "astro:content",
        "collections",
        "getCollection"
      ],
      "expected_output_excludes": [
        "fs.readFileSync",
        "manual markdown parsing"
      ]
    },
    {
      "id": "islands-hydration",
      "description": "Verifies the skill correctly uses island hydration directives",
      "input": "I have a React counter component that should only hydrate when the user scrolls to it",
      "expected_output_contains": [
        "client:visible",
        "client:load",
        "client:idle"
      ],
      "expected_output_excludes": [
        "client:only without purpose",
        "always hydrate everything"
      ]
    },
    {
      "id": "api-endpoint",
      "description": "Verifies the skill creates an Astro API route with correct APIRoute typing",
      "input": "Create an Astro API endpoint that handles POST requests for a contact form",
      "expected_output_contains": [
        "APIRoute",
        "export const POST",
        "request.json",
        "Response"
      ],
      "expected_output_excludes": [
        "express",
        "next/server"
      ]
    }
  ]
}
