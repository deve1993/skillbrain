{
  "skill": "iubenda",
  "version": "1.0.0",
  "test_cases": [
    {
      "id": "iubenda-banner-setup",
      "description": "Verifies the Iubenda cookie banner component uses correct scripts and env vars",
      "input": "Crea il componente IubendaCookieBanner per Next.js con supporto multilingua",
      "expected_output_contains": ["NEXT_PUBLIC_IUBENDA_SITE_ID", "cs.iubenda.com/autoblocking", "beforeInteractive", "perPurposeConsent", "rejectButtonDisplay"],
      "expected_output_excludes": ["hardcoded site ID", "afterInteractive for autoblocking", "no reject button"]
    },
    {
      "id": "iubenda-consent-mode-v2",
      "description": "Verifies Google Consent Mode v2 is enabled via the googleConsentMode template setting",
      "input": "Come abilito Google Consent Mode v2 con Iubenda?",
      "expected_output_contains": ["googleConsentMode", "template", "marzo 2024", "analytics_storage", "ad_storage"],
      "expected_output_excludes": ["manual gtag consent", "not required", "ignore consent mode"]
    },
    {
      "id": "iubenda-use-consent-hook",
      "description": "Verifies the useConsent hook pattern for conditionally loading scripts",
      "input": "Come carico GA4 solo dopo che l'utente ha dato il consenso analytics con Iubenda?",
      "expected_output_contains": ["useConsent", "_iub.cs.consent", "purposes", "analytics", "_iub_cs_consent_given"],
      "expected_output_excludes": ["load always", "ignore consent", "localStorage only"]
    }
  ]
}
