{
  "skill": "remotion",
  "version": "1.0.0",
  "test_cases": [
    {
      "id": "interpolate-clamp",
      "description": "Verifies ALWAYS use clamp on interpolate is enforced as a best practice",
      "input": "How do I animate a title sliding in from below over 30 frames in Remotion?",
      "expected_output_contains": [
        "interpolate",
        "extrapolateRight",
        "clamp",
        "useCurrentFrame"
      ],
      "expected_output_excludes": [
        "no clamp needed",
        "skip extrapolate"
      ]
    },
    {
      "id": "zod-schema-composition",
      "description": "Verifies Zod schema pattern is used for typed Remotion compositions",
      "input": "Create a Remotion video composition that accepts a title, subtitle, and brand color as props",
      "expected_output_contains": [
        "zod",
        "z.object",
        "zColor",
        "defaultProps"
      ],
      "expected_output_excludes": [
        "any type",
        "untyped props"
      ]
    },
    {
      "id": "docker-debian",
      "description": "Verifies Docker setup uses Debian not Alpine as per skill rules",
      "input": "How do I dockerize my Remotion rendering server?",
      "expected_output_contains": [
        "debian",
        "bookworm",
        "remotion browser ensure"
      ],
      "expected_output_excludes": [
        "alpine",
        "alpine:latest"
      ]
    }
  ]
}
