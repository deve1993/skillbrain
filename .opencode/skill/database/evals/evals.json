{
  "skill": "database",
  "version": "1.0.0",
  "test_cases": [
    {
      "id": "prisma-singleton",
      "description": "Verifies the skill creates a Prisma client singleton pattern to prevent hot-reload connection issues",
      "input": "Create a Prisma client setup for a Next.js app",
      "expected_output_contains": [
        "globalThis",
        "globalForPrisma",
        "PrismaClient",
        "NODE_ENV !== 'production'"
      ],
      "expected_output_excludes": [
        "new PrismaClient() every time",
        "no singleton"
      ]
    },
    {
      "id": "type-safe-queries",
      "description": "Verifies the skill demonstrates type-safe query patterns using Prisma payload types",
      "input": "How do I type a Post that includes its Author relation in TypeScript?",
      "expected_output_contains": [
        "Prisma.PostGetPayload",
        "include",
        "author: true",
        "PostWithAuthor"
      ],
      "expected_output_excludes": [
        "any",
        "as unknown",
        "no types needed"
      ]
    },
    {
      "id": "pagination-pattern",
      "description": "Verifies the skill implements cursor-based or offset pagination with total count",
      "input": "Create a reusable pagination utility for Prisma queries",
      "expected_output_contains": [
        "take",
        "skip",
        "count",
        "totalPages",
        "hasNext",
        "hasPrev"
      ],
      "expected_output_excludes": [
        "return all records",
        "no pagination needed"
      ]
    }
  ]
}
