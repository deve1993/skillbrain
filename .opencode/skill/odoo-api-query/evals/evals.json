{
  "skill": "odoo-api-query",
  "version": "1.0.0",
  "test_cases": [
    {
      "id": "discover-before-query",
      "description": "Verifies discovery-first approach before composing queries",
      "input": "Voglio cercare tutti i partner italiani in Odoo. Da dove inizio?",
      "expected_output_contains": ["inspect_model", "list_models", "DISCOVER", "country_id", "res.country"],
      "expected_output_excludes": ["execute immediately", "search_read res.partner --domain"]
    },
    {
      "id": "correct-domain-syntax",
      "description": "Verifies correct Odoo domain syntax with OR operator",
      "input": "Come scrivo un domain Odoo per trovare partner con email Gmail OPPURE Yahoo?",
      "expected_output_contains": ["\"|\"", "ilike", "gmail", "yahoo", "prefix notation"],
      "expected_output_excludes": ["WHERE", "OR", "SQL", "AND without prefix"]
    },
    {
      "id": "field-type-value-mapping",
      "description": "Verifies correct value types for different Odoo field types",
      "input": "Come passo il valore per un campo many2one vs boolean in un domain Odoo?",
      "expected_output_contains": ["int", "ID", "true", "false", "many2one", "boolean"],
      "expected_output_excludes": ["\"true\"", "\"false\"", "string for boolean"]
    }
  ]
}
