Migration file verification:
- CREATE TABLE count: 4
- Tables created:
  1. skills (with slug UNIQUE, indexes, RLS enabled)
  2. sessions (with FK to skills, expires_at index, RLS enabled)
  3. leads (with FK to sessions and skills, qualified_at index, RLS enabled)
  4. webhook_logs (with FK to sessions and skills, RLS enabled)
- All tables have uuid PK with gen_random_uuid() default
- All tables have created_at timestamptz with now() default
- Foreign keys use ON DELETE CASCADE
- Row Level Security enabled on all tables
- Indexes created for performance on frequently queried columns
