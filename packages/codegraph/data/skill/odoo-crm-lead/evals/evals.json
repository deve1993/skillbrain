{
  "skill": "odoo-crm-lead",
  "version": "1.0.0",
  "test_cases": [
    {
      "id": "server-action-no-cors",
      "description": "Verifies that Odoo calls are made server-side to avoid CORS",
      "input": "Come invio dati da un form React a Odoo CRM senza problemi CORS?",
      "expected_output_contains": ["'use server'", "Server Action", "server-side", "CORS"],
      "expected_output_excludes": ["fetch from client", "'use client'", "window.fetch", "axios from browser"]
    },
    {
      "id": "odoo-form-component",
      "description": "Verifies OdooLeadForm component uses useActionState correctly",
      "input": "Crea il componente form per inviare lead all'endpoint 'contatto-sito-x' di Odoo",
      "expected_output_contains": ["OdooLeadForm", "endpoint", "useActionState", "formAction", "submitToOdoo"],
      "expected_output_excludes": ["direct fetch from client", "useState for submission"]
    },
    {
      "id": "env-vars-odoo",
      "description": "Verifies correct env var naming (API key server-side only)",
      "input": "Quali variabili d'ambiente devo configurare per integrare Odoo CRM?",
      "expected_output_contains": ["NEXT_PUBLIC_ODOO_URL", "ODOO_API_KEY", "server-side", "NEXT_PUBLIC_"],
      "expected_output_excludes": ["NEXT_PUBLIC_ODOO_API_KEY", "expose api key to client"]
    }
  ]
}
