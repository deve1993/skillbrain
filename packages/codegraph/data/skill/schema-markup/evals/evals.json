{
  "skill": "schema-markup",
  "version": "1.1.0",
  "test_cases": [
    {
      "id": "json-ld-format",
      "description": "Verifies skill uses JSON-LD format and places it correctly in the document",
      "input": "How should I implement Organization schema on my company homepage?",
      "expected_output_contains": [
        "JSON-LD",
        "@context",
        "@type",
        "Organization",
        "name",
        "url"
      ],
      "expected_output_excludes": [
        "microdata",
        "RDFa"
      ]
    },
    {
      "id": "graph-combination",
      "description": "Verifies @graph pattern is used when combining multiple schema types",
      "input": "My homepage needs Organization, WebSite, and BreadcrumbList schema — how do I add them all?",
      "expected_output_contains": [
        "@graph",
        "Organization",
        "WebSite",
        "BreadcrumbList"
      ],
      "expected_output_excludes": [
        "three separate script tags",
        "pick only one"
      ]
    },
    {
      "id": "validation-tools",
      "description": "Verifies skill recommends Rich Results Test and Search Console for validation",
      "input": "How do I test if my schema markup is working correctly?",
      "expected_output_contains": [
        "Rich Results Test",
        "Search Console",
        "validate"
      ],
      "expected_output_excludes": [
        "no need to validate",
        "just deploy it"
      ]
    }
  ]
}
